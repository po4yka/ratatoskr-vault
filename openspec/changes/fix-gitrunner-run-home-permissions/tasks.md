## 1. Reproduce the failure

- [x] 1.1 Observe the `gate` job fail on `main` (run [32992183307](https://github.com/po4yka/ratatoskr-vault/actions/runs/32992183307/job/98252201490)); this is a defect in already-shipped code, so the existing failing tests (`crates/gitrunner/tests/credentials.rs::leaked_secret_is_redacted_from_output` and `::secret_material_absent_from_argv_and_environment`) are the failing test rather than new ones.
- [x] 1.2 Reproduce locally under the same `umask 022` the CI runner uses (this machine's own build tooling sandboxes cargo test invocations under a stricter `umask 077` that accidentally hides the bug, so the umask must be set explicitly): `umask 022 && ~/.cargo/bin/cargo test -p ratatoskr-vault-gitrunner --locked --test credentials -- --nocapture`, invoking the unwrapped real Cargo directly to bypass that local sandboxing. Confirmed both tests panic with `CredentialWriteFailed { reason: "run directory ... is group/world accessible" }`, matching the CI failure exactly.

## 2. Fix the run-directory creation

- [x] 2.1 Add `pub(crate) fn create_private_dir_all` in `crates/gitrunner/src/credentials.rs`, creating directories with `DirBuilder::new().recursive(true).mode(0o700)` on Unix so the mode is set by the creating `mkdir` itself, not derived from the ambient umask; existing directories are left untouched.
- [x] 2.2 Use it at both places this crate creates the run-home directory: `credentials::prepare_secret_file` (was a bare `create_dir_all`) and `GitRunner::run`'s best-effort `HOME` creation (`crates/gitrunner/src/lib.rs`, was also a bare `create_dir_all`). Audited the rest of the crate for other directory-creation sites that could hold or sit above credential material (`confine.rs`, `env.rs`, `spawn.rs`, `command.rs`, `error.rs`, `outcome.rs`, `url.rs`); none exist outside `confine.rs`'s test-only fixtures, which never run in production and hold no credential material.
- [x] 2.3 Keep the existing group/world-accessibility check in `prepare_secret_file` as defense in depth for an operator-supplied run directory that already existed before this call.

## 3. Verify the repair

- [x] 3.1 Re-run the same reproduction command from 1.2 (`umask 022`, unwrapped Cargo, `crates/gitrunner/tests/credentials.rs`): all three tests in that file now pass.
- [x] 3.2 Confirm with a real `stat` that the created directory's mode does not depend on the umask: built a standalone `DirBuilder::new().recursive(true).mode(0o700)` creation and exercised it under `umask 000`, `002`, `022`, and `077`; every run produced an on-disk mode of `700`.
- [x] 3.3 Confirmed via `git stash` that the pre-existing `Connect(PoolTimedOut)` failures in `services/vault/tests/boot.rs`, `mirror_lifecycle.rs`, and `reconcile.rs` reproduce identically on unmodified `main` — this machine's shared Postgres container is under contention from other concurrent agents, unrelated to `crates/gitrunner` and unaffected by this change in either direction.
- [x] 3.4 Run the full documented local gate: `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo deny check`, `cargo build --workspace --locked`, and `cargo build --workspace --locked --release` all pass clean. `cargo test --workspace --locked` passes for every crate that does not open a Postgres connection, and for the entire `ratatoskr-vault-gitrunner` crate this change touches (`cargo test -p ratatoskr-vault-gitrunner --locked`: 21 tests, 0 failed). Every database-backed integration test (`boot`, `mirror_lifecycle`, `reconcile`, and the `ratatoskr-vault-persistence` suite) fails with `Connect(PoolTimedOut)` or a raw socket `TimedOut`, which reproduces identically on unmodified `main` per 3.3 — the pre-existing host-contention exception this task tracks separately, not a regression from this change.
- [x] 3.5 Run `openspec validate --all --strict`: `Totals: 9 passed, 0 failed (9 items)`.
- [x] 3.6 Run `uvx zizmor@1.29.0 --persona pedantic --min-severity low .github/workflows/`: `No findings to report. Good job! (6 ignored)`.
